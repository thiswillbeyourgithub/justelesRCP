# Large source data + generated site are never committed
/data/
/dist/

# Self-hosted semantic-search assets fetched by download-model.sh (~120 MB):
# transformers.js + onnxruntime-web wasm, and the ONNX encoder. build.py mirrors
# them into dist/ at build time; like data/, they are large and never committed.
/vendor/
/models/

# Local, machine-specific Claude notes
claude.local.md
TODO.md
.serena
.tokensave

# Local runtime config (real analytics ids); docker/env.example is the template
/docker/.env

# Environment-specific deploy script (rsync working tree to a VPS + compose up)
/deploy.sh

# Environment-specific: wipe scraped overlays + manifests to restart scraping from 0
/reset-scrape-data.sh

# Python
__pycache__/
*.pyc
.venv/
.uvcache/

# OS / editor
.DS_Store
